-- =============================================================================
-- 최애(bias) 컬럼 추가 — profiles.bias_character
--
-- 사용자가 9명 중 한 멤버를 '최애'로 지정. 선톡 발신자 우선순위 +
-- 캐릭터 프롬프트(특별 애정 / 다른 멤버의 가벼운 질투)에 사용.
-- nullable — 미지정 가능. IF NOT EXISTS 로 멱등.
-- =============================================================================

ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS bias_character text;
